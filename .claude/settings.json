{
  "hooks": {
    "SessionStart": [
      {
        "command": "cd /home/user/Origin && git fetch origin main 2>/dev/null && git merge origin/main --no-edit 2>&1 | tail -3; cargo build --workspace 2>&1 | tail -5",
        "timeout": 120000
      }
    ]
  }
}
