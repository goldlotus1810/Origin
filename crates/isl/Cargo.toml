[package]
name    = "isl"
version = "0.1.0"
edition = "2021"

[features]
default = []
encrypt = ["aes-gcm"]

[dependencies]
olang   = { path = "../olang" }
aes-gcm = { workspace = true, optional = true }

[lib]
name = "isl"
path = "src/lib.rs"
